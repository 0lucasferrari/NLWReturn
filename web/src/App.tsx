import { Widget } from "./components/Widget"

function App() {
  return <Widget />
}

// A component is a function that returns HTML. A component always has its first 
// letter in uppercase.

// To run the project, type in the terminal: npm run dev

export default App

